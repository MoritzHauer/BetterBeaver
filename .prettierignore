node_modules
dist
coverage
pnpm-lock.yaml
docs/plans

# Git-ignored scratch space (throwaway browser-check scripts, screenshots).
*.local/

# Unit notes use a hand-rolled markup convention (*single-asterisk* =
# tappable target-language span, plan 0006 note-rendering fix); prettier's
# markdown emphasis normalization (* -> _) would corrupt it.
content/**/notes/*.md
