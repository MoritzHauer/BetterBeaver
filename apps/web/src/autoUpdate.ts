/** Settings toggle: apply a found content update immediately on startup
 * instead of waiting for a tap on the update banner. Off by default —
 * matches the opt-in spirit of plan 0012 §6. */
export const AUTO_UPDATE_KEY = "bb.autoUpdate";
